mod Array
  fn size : array
    `:array:.length`
  end

  fn at : array, index
   `:array:[:index:]`
  end

  fn each : array, f
    i = 0
    while i < Array.size(array)
      item = Array.at(array, i)
      f(item)
      i = i + 1  
    end
  end
end